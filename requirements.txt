# TITAN APP T3 - Dependencies
# Python 3.10+ recommended (tested with 3.11, 3.14)
# Cài đặt: pip install -r requirements.txt

# --- Web framework ---
Flask>=3.0.0
flask-session>=0.5.0
Flask-Caching>=2.1.0
Flask-WTF>=1.2.0

# --- Database ---
SQLAlchemy>=2.0.0
pyodbc>=5.0.0
pandas>=2.0.0

# --- Redis (session + cache) ---
redis>=5.0.0

# --- Production server & scheduler ---
waitress>=3.0.0
APScheduler>=3.10.0
schedule>=1.2.0

# --- File & upload ---
python-magic>=0.4.27
Werkzeug>=3.0.0

# --- AI (Gemini) ---
google-generativeai>=0.8.0

# --- PDF (Training / Library) ---
PyPDF2>=3.0.0

# --- RAG / embedding (Chatbot) ---
numpy>=1.24.0

# --- CSRF & forms (thường đi kèm Flask-WTF) ---
# WTForms đã nằm trong Flask-WTF
